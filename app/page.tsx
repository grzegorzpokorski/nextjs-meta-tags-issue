import type { Metadata } from "next";

export function generateMetadata(): Metadata {
  return {
    title: "another",
    description: "desc",
  };
}

export default function Page() {
  return <></>;
}
